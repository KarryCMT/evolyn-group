{
  "private": true,
  "name": "evolyn-group",
  "scripts": {
    "dev:web": "npm --prefix apps/evolyn-web run dev",
    "dev:core": "cd apps/evolyn-core && make run",
    "build:web": "npm --prefix apps/evolyn-web run build",
    "test:core": "cd apps/evolyn-core && make test"
  }
}
