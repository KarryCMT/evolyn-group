# Tasks

## 1. 协议与存储基线

- [x] 1.1 对齐 Go `LabelSchema 1.0` 与前端 `@evolyn.do/label` 的字段、默认值、边界和校验问题格式，补齐跨端一致性用例，并通过 Go 标签内核测试及 `pnpm --filter @evolyn.do/label test` 验证。
- [x] 1.2 审查并完善标签模板与不可变版本迁移，确认迁移序号、外键、检查约束、软删除条件唯一索引、管理员权限补授及 down 回滚完全对称，并通过迁移测试验证 up/down/up。
- [x] 1.3 收敛模板和版本持久化模型，确保租户字段、公开编码、草稿修订、预览修订、发布修订和版本唯一性与迁移一致，并通过仓储层真库测试验证同表单唯一绑定及软删除后重新绑定。

## 2. 后端模板生命周期

- [x] 2.1 完成模板创建、游标列表、筛选、详情和软删除服务，统一校验当前租户成员与 `label-templates` 操作权限，并以集成测试验证无权限拒绝、跨租户不可见和删除后查询不可见。
- [x] 2.2 完成草稿完整协议校验和 `draftRevision` Compare-And-Swap 保存，确保来源表单不可切换、页面投影同步更新且过期写入不覆盖新草稿，并通过 service 与真库并发测试验证。
- [x] 2.3 完成真实数据预览准入链路，通过表单窄端口执行记录和字段权限裁剪，仅在当前草稿成功渲染后写入精确修订标记，并测试无记录权限、无字段权限和预览期间并发保存三个拒绝场景。
- [x] 2.4 完成发布事务的模板行锁、当前修订复核、真实预览门槛、已发布表单和字段引用复核、不可变快照创建及发布指针推进，并测试事务回滚、同修订幂等重试与并发发布只产生一个快照。
- [x] 2.5 完成 HTTP 路由、DTO、稳定业务错误码、RBAC 资源注册、服务装配和成功操作审计，运行 `go test ./internal/platform/label/... ./internal/platform/server/...` 验证编译及服务测试。

## 3. 前端生命周期接入

- [x] 3.1 对齐标签模板 API DTO、公开编码路由、游标字段、草稿修订、预览修订、发布版本和全部稳定错误码，并通过 `pnpm --filter @evolyn.do/web typecheck` 验证类型一致性。
- [x] 3.2 完成表单扩展页面的模板懒创建/加载、草稿保存、真实数据预览和发布状态流；保存新草稿后必须使旧预览准入失效，修订冲突必须提示刷新且不得自动覆盖，并补充对应组件测试。
- [x] 3.3 验证无权限、表单已绑定、协议无效、真实预览缺失和修订冲突的前端反馈使用统一错误处理且不泄露受限数据，并通过 `pnpm --filter @evolyn.do/web test` 覆盖关键交互。

## 4. 集成验证与文档收口

- [x] 4.1 在 PostgreSQL 集成环境验证创建 V1、保存、真实预览、发布 V1、修改并重新预览、发布 V2、V1 快照不变、删除及重新绑定的完整链路，并保留测试结果。
- [x] 4.2 执行跨租户模板编码探测、无资源权限管理、无记录权限预览和无字段权限预览安全回归，确认响应不泄露其他租户、记录或字段数据。
- [x] 4.3 运行标签相关 Go 测试、前端标签包测试、Web 类型检查与测试；记录任何因环境缺失而未执行的检查及原因，全部通过后再勾选本任务。
- [x] 4.4 更新二维码标签总体设计文档和项目导航中的实际完成范围、接口及后续拆分边界，并运行 `openspec validate add-label-template-lifecycle --strict` 完成规格、设计、任务和实现的一致性复核。

## 验证记录（2026-09-23）

- 已通过：标签相关 Go 测试、`@evolyn.do/label` 全量测试（4/4）、Web 全量测试（31/31）、Web 类型检查、OpenSpec strict 校验。
- 已在本机 PostgreSQL 14（`127.0.0.1:5432`）通过真库验证：仓储唯一绑定/软删除重绑、完整 V1→V2 生命周期、V1 快照不可变、CAS 与预览并发保护、发布回滚/幂等/并发单快照、租户与权限隔离，以及迁移快照零重放和 000084 down/up/down/up 对称性。对应后端套件 `go test -count=1 ./internal/infrastructure ./internal/engine/label ./internal/platform/label/... ./internal/platform/server/...` 全部通过。
- Web 原有 5 个失败已收口：数据源断言允许成员引用内部投影，筛选面板测试按当前 Element Plus 事件契约驱动组件；定向测试 9/9、全量测试 31/31 均通过。
