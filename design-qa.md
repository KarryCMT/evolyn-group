# 连线添加下级节点与紧凑浮窗验收

- 验收日期：2026-09-25
- source visual truth path：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-f4219f0c-306e-4ca9-a154-f85b87f215f0.png`（1567 × 1188）、`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-11d57deb-b1c3-4bf6-8c18-600f3d45fd76.png`（1656 × 1141）。
- implementation screenshot path：Codex 内置浏览器 `tab 1` 实时渲染，页面地址 `http://127.0.0.1:4178/`；临时验收壳位于 `/private/tmp/evolyn-intelligent-qa`，不写入仓库。
- viewport：543 × 656 CSS px，devicePixelRatio 1；参考图为桌面裁切，按控件尺寸、层级、交互和窄屏收敛行为归一化比较。
- state：连线圆形加号、紧凑节点菜单、选择新增数据、节点插入完成、手动点击新节点打开配置抽屉。

## Findings

- 首轮交互复现了“点击菜单后画布不更新”：自定义边卸载时调用了不存在的父类生命周期方法，异常中断 LogicFlow 重绘；已移除错误调用并完成回归。
- 浮窗采用禁用粘贴入口、数据处理双列目录、折叠更多节点、外部点击与 Esc 关闭；窄屏自动单列，未出现横向溢出。
- 点击连线菜单中的“新增数据”后，节点插入在所点连线位置，原目标节点及后续节点下移，连线重新接续。
- 添加完成后属性抽屉保持关闭、删除按钮保持禁用；用户手动点击新节点后，属性抽屉才打开并启用删除操作。
- 新一轮运行时日志无业务错误；仅临时验收壳的 Vue feature flag 构建提示，不影响仓库应用构建。

## Primary interactions tested

1. 点击任一连线中点的圆形加号，打开对应位置的紧凑节点浮窗。
2. 点击“新增数据”，浮窗关闭并在该连线上插入节点。
3. 验证新节点未自动选中、属性抽屉未自动打开。
4. 手动点击新节点，属性抽屉正常打开。
5. 无效连线 id 自动回退为在结束节点前追加，避免无反馈。

final result: passed

---

# 修改数据节点前端交互验收

- 验收日期：2026-09-25
- source visual truth path：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-2bd05325-5902-47e9-b2dd-3360a244b145.png`（1743 × 1524）、`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-1348aacb-7f76-4680-b9f2-46861c580820.png`（1419 × 1419）、`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-51194caa-2294-45cb-9dbb-e76365ec01cb.png`（1432 × 1386）；其余 8 张交互态截图来自同一用户附件目录。
- implementation screenshot path：Codex 内置浏览器 `tab 1` 的浏览器渲染截图，页面地址 `http://127.0.0.1:4178/`（临时验收壳位于 `/private/tmp/evolyn-intelligent-preview`，不写入仓库）。
- viewport：桌面 1440 × 1200 CSS px、窄屏 760 × 900 CSS px。
- density normalization：实现截图 devicePixelRatio 1，截图像素与 CSS 视口一致；参考图为不同裁切和密度的产品截图，因此按内容区域、控件比例和交互状态归一化比较，不以浏览器外框或画布裁切作为差异。
- state：表单修改初始错误态、修改对象类型菜单、表单选择菜单、字段菜单、运算符菜单、值来源菜单、节点字段树、字段赋值、切换保护确认框、前序节点修改模式、窄屏布局。

## Findings

- 未发现可执行的 P0、P1 或 P2 差异。
- 字体与排版：使用 `Inter`、`PingFang SC`、`Microsoft YaHei` 回退链；标题、标签、正文、错误文案的字号和字重层级与参考图一致，未出现截断或异常换行。
- 间距与布局节奏：桌面态保持双列修改对象、四段式条件行和三段式字段赋值行；分区留白、灰色分隔带、圆角、阴影和下拉层级与参考图一致；760px 窄屏无横向溢出。
- 颜色与视觉 token：主文本、次文本、青绿色交互态、红色校验态、浅灰边框和白色弹层均与参考图同一语义层级，焦点和错误边界可区分。
- 图像与资产：界面不包含照片或品牌插画；可见图标全部使用项目既有 Remix Icon / 组件图标，没有用 emoji、文字字形或手绘 SVG 替代。
- 文案与内容：修改对象、筛选条件、无匹配新增、设置字段值、快捷填充、节点字段值、自定义、空值等文案与参考图一致。

## Full-view comparison evidence

- 1440 × 1200 桌面浏览器截图与参考图 1/2 对照：标题区、节点名称、修改对象、筛选条件、字段赋值四个主要区域的层级、顺序和宽面板构图一致。
- 760 × 900 窄屏截图验证：属性面板占满可用宽度，修改对象上下排列，内容区可滚动，未遮挡固定标题或关键操作。

## Focused region comparison evidence

- 对照参考图 3/4/5/6/7/8/9/10/11，逐项打开并检查了目标表单、全部/任一、字段、六种运算符、节点字段值/自定义/空值、节点字段树和字段赋值弹层。
- 浏览器实测了表单模式切换至节点模式的清空确认；确认后筛选区正确隐藏，前序“新增数据 · 员工档案”可选，字段赋值区保留为唯一必填区。

## Primary interactions tested

1. 修改对象类型在“选择表单修改数据 / 选择节点修改数据”间切换，并在已有依赖配置时弹出二次确认。
2. 目标表单搜索和选择、条件“所有 / 任一”、添加/删除条件、字段搜索、六种运算符和无匹配时新增开关。
3. 条件值支持节点字段与自定义值；字段赋值支持节点字段、自定义、空值，并提供兼容字段推荐树。
4. 添加/删除字段、目标字段去重、快捷填充和节点说明实时更新。
5. 浏览器控制台错误与警告检查结果为空数组。

## Comparison history

- 第一轮全视图和聚焦交互态比较未发现 P0/P1/P2 问题，因此没有触发视觉修复迭代。

## Implementation checklist

- [x] 表单/节点两种修改对象模式
- [x] 条件组合、运算符和值来源交互
- [x] 字段赋值、空值和快捷填充
- [x] 依赖配置切换保护
- [x] 桌面与窄屏布局
- [x] 控制台错误检查

final result: passed

---

# 历史记录：智能助手 Pro 前端与画布设计器验收

- 验收日期：2026-09-24
- 最终结果：passed
- 验收视口：1500 × 800、633 × 599 CSS px，devicePixelRatio 1
- 实现截图：Codex 内置浏览器实时截图（临时免登录验收壳已在验收后移除，未写入仓库）

## 参考图

- 空状态：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-c22046ea-cbe0-4886-8269-ff37dfbf61ce.png`（3007 × 1542）
- 新建弹窗：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-d0280124-35da-4d85-ab6e-56a0db2c4d58.png`（1430 × 1341）
- 引导第一步：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-19f85255-79a2-4437-a91e-a1ac00755ce3.png`（1533 × 1090）
- 引导第二步：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-40606ada-f5f6-4b59-b415-fb46339267cf.png`（1531 × 1115）
- 画布设计器：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-b73689f6-04d0-45de-83bf-cd9af8a108e7.png`（3024 × 1543）
- 创建确认：`/var/folders/s4/f6855yyx2j70w2r7crpmnv7h0000gn/T/codex-clipboard-894867f6-0630-44ac-9576-af0c586c23ef.png`（2988 × 1516）

## 对比结论

| 区域 | 结果 | 说明 |
| --- | --- | --- |
| 页面结构 | 通过 | 顶部说明、主按钮、双引导卡、分隔线与底部注释的层级和间距与参考一致。 |
| 新建弹窗 | 通过 | 名称、触发方式、表单下拉、更多触发、标签编辑和底部操作区均已还原。 |
| 功能引导 | 通过 | 两栏配置、动态节点预览、下一步与节点说明页均与参考保持同一信息架构。 |
| 交互状态 | 通过 | 已验证表单下拉、HTTP 触发展开、标签创建、动作选择、两步引导和创建后列表。 |
| 画布设计器 | 通过 | 顶部工作区、工具栏、原项目网格资源、触发/执行/结束节点及连线与参考图保持同一结构。 |
| 画布交互 | 通过 | 已验证 `LogicPanel.vue` 实际渲染、节点菜单、属性面板、添加/删除、撤销/重做、缩放、居中以及创建后路由跳转。 |
| 包边界 | 通过 | 画布文档模型、上下文/历史、节点、工具面板、LogicFlow 投影和完整设计器均由 `@evolyn.do/intelligent` 组织，宿主页仅保留路由与持久化适配。 |
| 响应式约束 | 通过 | 弹窗宽高随视口收敛，内容区独立滚动，窄屏切换为单列。 |

## 迭代记录

1. 初次浏览器检查发现全局暗色变量覆盖弹窗背景与按钮，已为两个弹窗建立独立浅色视觉边界。
2. 初次检查发现弹窗内部选择卡片产生横向溢出，已统一盒模型并关闭横向滚动。
3. 初次检查发现大弹窗在 800px 高视口下超出底部，已按视口限制内容区高度并保留内部滚动。
4. 标签编辑器位于长内容底部时可能被遮挡，已在打开时自动滚动到可见区域。
5. 初次画布检查发现旧 Vue 节点注册器会在缩放后反向放大节点模型，已改为 Vue 3 `createApp` 驱动的固定尺寸 LogicFlow HTML 节点。
6. 窄视口下初始图可能偏离可视区，已在首次渲染与结构变化时按文档几何中心定位。
7. 将原项目的 `components`、`context`、`icon`、`mock`、`node`、`tool`、`util` 和 `assets` 收敛到 Vue 3 + TypeScript 的设计器链路，主设计器直接使用 `components/LogicPanel.vue`。

## 备注

- 保存与启用当前为前端占位反馈，待智能助手服务端契约落地后接入持久化接口。
- 临时免登录验收壳已移除；本地后端未启动产生鉴权接口连接提示，不影响画布功能。
