# 数据联动设计核对

## 参考

- 用户提供的 15 张数据联动配置截图。
- `docs/低代码平台/表单设计器/数据联动前后端功能设计开发文档_V1.0.md`。

## 已核对

- 字段属性中的“默认值 → 数据联动”入口，以及“数据联动设置 / 已设置数据联动”状态。
- 弹窗的信息层级：联动表单、ALL/ANY 条件、动态操作符、当前字段/自定义值、多个回填映射、初始化和无结果策略。
- 弹窗宽度、分区留白、主按钮层级、可搜索下拉框与窄屏条件行换行。
- 未完成配置的前端即时提示，以及保存/发布阶段的协议校验和循环依赖阻断。

## 自动验证

- `@evolyn.do/form` 类型检查、单元测试和构建通过。
- `@evolyn.do/web` 类型检查和生产构建通过。
- 使用用户已登录的本地 Chrome 页面完成实际状态复核：入口按钮文字正常显示，弹窗通过 Teleport 居中覆盖设计器，条件与映射控件保持横向栅格布局。
- 未配置状态与参考图一致：默认值选择“数据联动”后保持该模式，并显示全宽“数据联动设置”入口；仅保存完整规则后切换为“已设置数据联动”。
- 实际交互发现并修复 Vue Proxy 传入 `structuredClone` 引发的 `DataCloneError`；重新加载后控制台不再出现该运行错误。

## 后续人工验收路径

打开任一已发布普通表单的设计页，选择单行或多行文本字段，在右侧“默认值”选择“数据联动”，依次检查联动表单选择、条件编辑、字段映射、保存、发布和运行时回填。

## 结论

final result: passed

---

# 关联其他表单数据设计核对（2026-09-22）

## 证据

- source visual truth path：用户提供的 9 张界面截图，原图分辨率从 `704×1280` 到 `2032×1477`不等。
- implementation screenshot path：无。本地 Web 可在 `http://127.0.0.1:11000/` 启动，但未认证会话被重定向到 `/auth/login?redirect=%2F`，无法到达需要已登录与已发布源表单数据的设计器状态。
- viewport：本地预览使用 Codex 内置浏览器默认桌面视口；因认证阻塞，未进入可对比状态。
- density normalization：未执行；没有获得实现截图。
- state：目标为下拉框的字段属性、源表/字段选择、排序下拉和过滤对话框；实际只能到达登录页。

## 自动化核对

- `@evolyn.do/form` 277 个测试通过，包括「只有 combo / combocheck 显示选项来源」、v13 Schema 校验、引用字段删除/改名保护。
- `@evolyn.do/form` 与 `@evolyn.do/web` TypeScript 类型检查通过。
- Go 表单服务、Controller 和请求权限路由测试通过。

## 全局与局部对比

- full-view comparison evidence：缺失，原因是本地设计器路由需要已认证会话。
- focused region comparison evidence：缺失；无法展开源字段和过滤对话框的实际浏览器状态。
- 字体/排版、间距/布局节奏、颜色 token、图标质量和界面文案均已在代码中按既有 Element Plus 与项目 token 复用，但没有浏览器截图无法判定视觉一致性是否通过。

## Findings

- [P2] 缺少相同状态的浏览器渲染证据。代码、类型和组件行为测试已通过，但未能检查属性面板宽度、弹层定位、文字折行和对话框密度。

## Comparison history

- 第 1 次：启动本地 Vite 后访问根路由，被重定向登录页；未进行伪造认证或绕过权限。
- 修复：无可在本次任务范围内安全应用的视觉修复；需要可用的本地测试会话与源表数据。
- post-fix visual evidence：无。

## Implementation checklist

- 在已登录的本地环境打开表单设计器。
- 对 combo / combocheck 分别捕获自定义、关联源、排序和过滤对话框状态。
- 以与参考图同宽的属性面板进行一轮全局+局部对比，并检查控制台错误。

final result: blocked
